export const dayEatenProducts = state => state.day.day.eatenProducts;
export const dayId = state => state.day.day.id;

export const dayDaySummary = state => state.day.daySummary;
export const dayNextDaySummary = state => state.day.day;
